import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: "#463",
        alignItems: "center",
        justifyContent: "center"
    },
    
    textStyle: {
        color: "#fff",
        fontWeight: "bold",
        fontSize: 20
    }
});